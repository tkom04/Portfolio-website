import type { Metadata } from "next";
import { Inter } from "next/font/google";
import { Analytics } from "@vercel/analytics/react";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Web Developer Portfolio | Freelance Developer",
  description: "Professional web developer portfolio showcasing skills, projects, and client reviews. Available for freelance opportunities.",
  keywords: ["web developer", "freelance", "portfolio", "frontend developer", "full stack developer"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Analytics />
      </body>
    </html>
  );
}

